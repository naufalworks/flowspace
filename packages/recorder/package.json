{
  "name": "@flowspace/recorder",
  "version": "0.1.0",
  "description": "Records user actions and converts them to CVL tokens",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "jest",
    "dev": "tsc -b --watch"
  },
  "dependencies": {
    "@flowspace/core": "*",
    "playwright": "^1.40.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.0"
  }
}
